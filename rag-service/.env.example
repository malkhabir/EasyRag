# =============================================================================
# EasyRag RAG Service - Environment Configuration
# =============================================================================
# Copy this file to .env and customize for your setup:
#   cp .env.example .env
#
# All settings have sensible defaults. Only uncomment what you need to change.
# =============================================================================

# =============================================================================
# LLM CONFIGURATION (Ollama)
# =============================================================================
# The language model used for generating responses.
# Make sure to pull the model first: ollama pull <model_name>
#
# Recommended models:
#   - phi3:   Fast, excellent at understanding tables (DEFAULT)
#   - llama3: High quality responses, slower
#   - mistral: Good balance of speed and quality
#   - mixtral: Best quality, requires more resources
#
OLLAMA_MODEL=phi3
OLLAMA_HOST=localhost
OLLAMA_PORT=11435

# =============================================================================
# EMBEDDING MODEL
# =============================================================================
# The embedding model for converting text to vectors.
# Larger models = better quality, but slower and more memory.
#
# Options:
#   - BAAI/bge-m3:  1024 dims, multilingual (DEFAULT, RECOMMENDED)
#   - BAAI/bge-large-en-v1.5: 1024 dims, English-focused
#   - sentence-transformers/all-MiniLM-L6-v2: 384 dims, fast
#
# WARNING: If you change embedding dimensions, also update QDRANT_VECTOR_SIZE
# and recreate your Qdrant collection!
#
EMBEDDING_MODEL=BAAI/bge-m3

# =============================================================================
# VECTOR DATABASE (Qdrant)
# =============================================================================
# Qdrant stores your document vectors for semantic search.
#
QDRANT_HOST=localhost
QDRANT_PORT=6335
QDRANT_COLLECTION_NAME=accounting_docs
QDRANT_VECTOR_SIZE=1024

# =============================================================================
# SERVER CONFIGURATION
# =============================================================================
HOST=0.0.0.0
PORT=8080
DEBUG=false

# =============================================================================
# LOGGING
# =============================================================================
LOG_LEVEL=INFO
